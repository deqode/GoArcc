package authentication_test

import (
	. "github.com/onsi/ginkgo"
)

var _ = Describe("Login Test", func() {
	Describe("Login Test", func() {
		By("By Rpc Calls")

		Context("Get an error when request object is nil", func() {
			It("Failed precondition err must be responded", func() {

			})
		})

		Context("Get an error if url is empty", func() {
			It("Failed precondition err must be responded", func() {
			})
		})

		Context("Get an error if url is invalid", func() {
			It("Failed precondition err must be responded", func() {
			})
		})
	})
})
